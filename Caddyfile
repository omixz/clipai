# Caddy gets and renews an HTTPS certificate automatically (Let's Encrypt) for
# whatever hostname is here. A bare IP address cannot get a valid
# certificate; this needs the DuckDNS hostname pointed at the VM's public IP.
peakclip.duckdns.org {
	reverse_proxy app:8000
}
